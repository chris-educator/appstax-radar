# Explore this codebase (read-only)

Map **AppStax Radar** without editing files.

## Produce

1. **Purpose** — Scout agent, review queue, public feed
2. **Stack** — FastAPI, Vite, SQLite, Gemini
3. **Key paths** — `src/scout.py`, `src/sources.py`, `server/main.py`
4. **Run locally** — `npm run dev:api` (:8005) + `npm run dev:client` (:5187)
5. **Deploy** — `docs/DEPLOY.md`, `radar.appstax.ai`

Read-only. See `AGENTS.md`.
